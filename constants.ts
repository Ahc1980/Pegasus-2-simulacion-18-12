
export const SIM_SPEED_MS = 1000; // 1 second real = 1 hour sim
export const K_FRICTION = 0.045; // Friction coefficient for head loss Hf = K * Q^2
export const P_INLET_BASE = 65; // Base inlet pressure in meters
export const DEFAULT_TARGET_CP = 20; // Default target pressure at critical point
export const TELEMETRY_HOURS = [0, 6, 12, 18];
